header.header 
    nav.nav.nav--tours 
        a.nav__el(href='/views') All tours 
    .header__logo 
        img(src='/image/logo-white.png', alt='Natours')
    nav.nav.nav--user
        if user 
            a.nav__el.nav__el--logout Log Out 
            a.nav__el(href='/me')
              img.nav__user-img(src=`/image/users/${user.photo}` alt=`user.name`)
              span= user.name.split(' ')[0]
        else
            a.nav__el(href='/login')  Log in 
            a.nav__el.nav__el--cta(href='#') Sign up
        